sudo: false
language: node_js
node_js:
  - "9"
cache: yarn
script:
  - yarn build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GH_TOKEN
  local_dir: dist
  on:
    branch: master
notifications:
  email: never
